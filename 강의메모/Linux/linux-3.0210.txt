shadow file은 나중에 검색해보길 바라고

echo $SHLVL

==============================
greeting

cp ../HelloWorld .

bash에서 파일을 실행 시킬수 있다는걸 기억해라

=================================

리다이렉트 ll > ls.txt
cat ls.txt

ll의 결과가 ls.txt에 들어간다

date > date.txt

expr은 띄어쓰기 됨

===================================

echo 4^4^4 | bc는 본래값이 아니라 딴게 나온다
뒤에서부터 제곱되기 때문
==============================

if [ $a==$b ] 공백을 지켜주자
then
	echo
fi