자바스크립트를 node에서 쓸 수 있는것 REPL

언더바는 마지막결과

type pacakage.json

자동완성 어케하는거지

assertion 디버깅할때 값에 대한 이벨류에이션 해서 성공 못하면 멈춤

파워쉘 .\app.js


새로운 버퍼를 형성해서 buf1에 넘겨준다

버퍼에 가지고 있을 일이 많다(데이터를 2진수 자체로 쓰고자 할때)
자바스크립트는 버퍼가 없다 nodejs가 제공

그냥 찍으면 data chunk만 찍는것이다