do while문은 ;로 막아야한다
do while은 i < 0으로 해도 한번은 찍힌다

브렉 포인트 해주고 f10으로 진행

CPP는 프로젝트 이름 마음대로 바꾸면 안됨

arrays and pointer

변수는 그 메모리의 값이 아니라 주소를 가지고 있다


음악파일을 로딩했을때 배열식으로 한바이트 단위로 넣는게 아니야

동적할당을 이용해서 한번에 메모리에 올려

버퍼에 있는 메모리를 이동해가면서 읽는것임


피자라는 데이터를 가지는 여러개의 변수를 만드는 것은 낭비여
하나의 주소에 피자를 주고 참조하게 한다

포인터는 4바이트의 integer값이다

역 참조 : 주소에 들어가서 또 주소를 참조하기 때문에 역참조라고 한다

포인터를 쓰기위해서는 반드시 초기화 해서 써야한다 아니면 쓰레기값이 들어감

null로 안막아주면 할당되지 않은 메모리에 가기때문에 프로그램이 죽는다