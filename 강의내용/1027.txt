변수의 이름엔 $를 되도록 쓰지 말자(문자열 안에 변수를 입력하는 용도로 씀)

대입같은 경우는 오른쪽에서 왼쪽 c가 b에 대입되고 b가 a에 대입된다

c++에서 ++x는 전처리 x++는 후처리라고 한다

=== 3개씩 되는건 보통 자바스크립트에서만 있는 연산자 (타입까지 같은지 확인하기 때문에)

!는 not을 표현

> : greater than >= : greater than eqaul 
< : less than <= : less than eqaul       -> 나중에 리눅스에서 ge로 사용

==(eqaul)은 두 피 연산자 타입이 다르면 강제로 같게 한다.

비트 연산
1000 0001 (-1) 맨 앞이 부호를 나타냄
0000 0001 (+1)

1111 0001 &
1111 0101
1111 0001

0000 0001 (십진수 1)
0000 0010 (십진수 2) 왼쪽 쉬프트 곱셈이 된다

0000 1010 (십진수 10)
0000 0101 (십진수 5)오른쪽 쉬프트 나눗셈이 된다

typeof new Date() 메모리에 object를 생성하기 위해 new를 써줌
new는 instantiating 메모리에 할당해줌
메모리에 올라간 object는 instance

void는 어떠한 타입도 반환하지않음

void는 타입없이 선언할 때 사용
backgroundColor 에서 C는 대문자로

javascript는 ; 안붙여도 됨 하지만 c를 쓸경우 생각하고 문장의 끝을 표현해주기 위해 사용

break는 루프를 그만하고 빠져나가는것

자기를 한다음 break를 하지않으면 다음것으로 넘어가서 Thursday가 나옴

break와 continue의 차이
continue는 다음 것으로 넘어가지않고 다시 루프의 맨처음으로 돌아감