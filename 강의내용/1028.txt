공백이 생기면 그만큼 바이트 잡아먹는다

break과 continue 차이 알기

다른 언어는 0이라도 들어가야함 심지어 문자열은 0이 있으면 null이 되서 끊김

push pop = stack = first in last out
그릇에 담긴다고 생각하면 됨

shift = que가 first in first out

method는 object가 가지고 있는 함수의 기능을 의미