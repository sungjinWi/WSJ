타입(int, string)
자바와 자바스크립트는 다르다
자바스크립트는 웹브라우저가 한줄한줄 처리해줌

object orient programming
object 채우는게 setter 가져오는게 getter

ES6
arrow 문법 ->

ES8
async/await 동시다발적으로 일어나야하는 일들에대한 생김

하이브리드앱 - 겉은 앱인데 속은 다 웹임
mac에서 해야 안드로이드 ios 다 가능

MVC model(data의미) view(UI의미) controller 영역을 나누어 설게

XML(Extenede Markup Language)

런타임 - 실행시 기능의 구현이 정해진다

스레드(멀티 프로세싱) - 프로세스, 실행한 프로그램이 실행되는 공간에서 병렬 또는 백그라운드에서 작업을 돌린다
블로킹은 어떤일이 끝나길 기다리고 함
논블로킹은 안기다리고 내 일만 한다.
npm node package manager

@Restful이란 무엇인가

인터프리터 언어라서 순서가 중요함
밑에 정의한 것을 위에서 실행하려고 하면 실행할 수 없다.

java script에서 object 기능을 가지고 있는 변수(어떠한 일을 수행할 수 있는 함수까지 포함)
메모리를 참조하는 타입이라 reference

var abc = initialize; -> memory에 적재됨(load)
memory에서 끄집어내서 실행(excute)시켜주는게 reference타입이다.(직접 실행하는게 아님)

json 검색해보기

지역변수(Local variable)는 중괄호 안에서 선언하고 그 안에서만 사용가능
전역변수(Global variable)는 밖에서 선언해서 다 사용가능
변수를 식별 자(identification)라고 함
c에서 컴파일할떄 중간파일에 underscore 방식을 사용해서 컴파일러에 혼돈을 주지 않기 위해 camel

function-scoped
const(상수)- 변환되지 않는값이라 초기화 할때만 값을 줄수 있고 이후에 writing 없이 reading만 가능

디버깅 툴인 developement 툴에서 볼수 있는것 console.log

window.alert 팝업 윈도우에 띄운다

object가 사용하는 함수들에 대해서는 method
기능을 표현하는 것은 property

object가 memory에 ~~~~되면 @instance

car라는 object property 백미러 method 백미러 움직이는 버튼

@unix timestamp epoch time