alloc C , C++에서도 쓴다

console error를 계속 찍는 연습을 해서 오류를 체크하자

node --trace-deprecation ...` to show where the warning was created)

require로 가져오는 것들은 왠만하면 const로 쓰자

0은 8진수를 나타내기 위해 쓰는것 777은 모든 실행권한을 갖는 것이다
윈도우는 mode 안 쓴다

path 파일명이 아니라 그냥 path (c&p하느라 잘못 적으심)

in C, C++ 'a' only one "ab" string


어플리케이션은 화면에 뿌릴 데이터를 받아오는것이 아니라
http 서버를 수행하는 녀석에게 request를 보내서 보낸 내용에 대해 get해올수 있다
그런 어플리케이션들도 http client가 된다

jsp는 자바가 지원하는 java server page

윈도우는 jsp를 표방해서 서버페이지를 하기위해 asp active server page를 만들었다


자바스크립트는 프론트엔드 스크립트

nodejs를 통해 server 스크립트를 사용할 수 있다

80이 기본이기 때문에 충돌한다